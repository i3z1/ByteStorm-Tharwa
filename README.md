# ثَروة (Tharwa) — مساعد بنكي ذكي 🏦🤖

> **فريق ByteStorm** — هاكاثون امد 2026، أكاديمية طويق · مسار: الذكاء الاصطناعي التوليدي للتقنية المالية

مساعد بنكي توليدي يعمل داخل التطبيق البنكي: نفّذ التحويلات، وحلّل مصروفاتك، واحصل على توصيات استثمار — كلها **بأوامر طبيعية بالمحادثة**. يحوّل «ثَروة» اللغة الطبيعية إلى عمليات بنكية فعلية عبر **Function-Calling**.

## 🔗 النموذج التفاعلي المباشر
**جرّبه الآن:** https://tharwa-orcin.vercel.app

افتح شاشة «المساعد» من الشريط السفلي، ثم اكتب مثلاً:
- `حوّل 500 لأحمد` → يفهم الأمر، يطلب التأكيد، ثم **ينفّذ التحويل فعلياً** (ينقص الرصيد وتظهر العملية).
- `كم صرفت على المطاعم؟` → يجيب بأرقام حقيقية من بيانات العميل.
- `اقترح لي خطة استثمار` → يولّد توصية ويفتح شاشة الاستثمار.

## ✨ المزايا
- **تحويلات بالمحادثة** — أمر طبيعي واحد + خطوة تأكيد آمنة.
- **تحليل وتوصيات ذكية** — تحليل المصروفات، رؤى مالية، وخطط استثمار حسب مستوى المخاطرة.
- **شمول مالي** — استشارة مالية ذكية متاحة لكل عميل.

## 🧠 كيف يعمل (البنية)
```
واجهة التطبيق (HTML/JS)  ──►  /api/chat (Vercel Serverless)  ──►  Gemini (LLM) + Tools
        ▲                                                              │
        └──────────  رد + تحديث الحالة + تنفيذ الأداة  ◄───────────────┘
```
- يفهم نموذج **Google Gemini** أمر العميل بالعربية، ويستدعي الأداة المناسبة:
  - `execute_transfer(amount, recipient)` — ينفّذ التحويل بعد التأكيد.
  - `open_screen(screen)` — يفتح شاشة التحليل/الاستثمار.
- مفتاح الـ API محفوظ **كسر على الخادم** (`GEMINI_API_KEY`) ولا يظهر إطلاقاً في المتصفح.

## 🛠️ التقنيات
`Google Gemini (Function-Calling)` · `Vercel Serverless Functions` · `JavaScript / HTML` · `بيانات معاملات اصطناعية` · `معالجة اللغة الطبيعية`

## 👥 الفريق — ByteStorm
| العضو | الدور |
|---|---|
| عبدالعزيز أحمد | قائد الفريق · برمجة تطبيقات |
| عبدالرحمن العتيبي | مهندس ذكاء اصطناعي |
| أسامة الحربي | عالِم بيانات · تعلم الآلة |
| نايف المدخلي | مطوّر · مهندس بيانات |

## ⚙️ التشغيل محلياً
```bash
# أنشئ مفتاح Gemini المجاني: https://aistudio.google.com/apikey
# ثم ضعه في متغيّر بيئة (لا تضعه في الكود):
#   GEMINI_API_KEY=...
npm i -g vercel && vercel dev
```

## 📌 ملاحظة
هذا **نموذج أوّلي (~30% من المشروع)**: البيانات والمعاملات **اصطناعية/محاكاة** ولا يتصل بأي بنك حقيقي أو أموال حقيقية — وهو ما يتوافق مع مرحلة النموذج التجريبي.

---
*Built for the Amد 2026 hackathon by ByteStorm. A working prototype — simulated banking data, real generative-AI command understanding via Gemini function-calling.*
